# MEMORY.md — クロスフィルター加工ツール

実装中の決定事項・試して捨てた案・未解決点を、このファイル 1 つに時系列で追記する。設計書(DESIGN.md)の「決め」を変えたいときは、ここに理由を書いてから止まる。

## 2026-09-18 設計(Fable 5.1)

### 決定
- 試作版は `legacy/cross-filter-v0.html`(claude.ai アーティファクト 3f2bbcf2 の版をそのまま保存)。比較用に残し、テストフック `__cf` 以外は変更しない
- 違和感の主因を数値モデル(`tools/sim_streak.py`)で特定: 断面が矩形で白帯化、単一指数で先が急に消える、光源ごとの強度差が小さい。詳細は DESIGN.md §2
- 対策: A(断面を軟らかく)・B(2 成分減衰、裾は半解像度)・D(w²・lv³)を必須。C(にじみ量/面積で光源強度を推定)は実装するが既定オフ、比較画像を見てユーザーが採否を決める
- 「強さ」の内部マッピングを 0.03·100^g → 0.02·50^g に変更(上限 3.0 → 1.0)。localStorage キーは v3 → v4
- 「裾の伸び」スライダーを追加(β = 0.6·B/100、既定 50)
- 保存は結果ダイアログに「共有する」(Web Share、ファイル共有可のときだけ)を追加
- テストは Node + playwright-core + 手元の Edge(SwiftShader フラグ)。Chromium のダウンロードはしない
- 公開は独立 public リポジトリ + GitHub Pages。git 操作はユーザーが行う

### 試して捨てた案
- 「強さ」を「芯の明るさ」と「裾の伸び」の 2 本に分ける(ブリーフ A 前半): 主要スライダーの体系が変わる。裾は「こまかい調整」に 1 本足すだけにした
- 飽和前の圧縮カーブ(ブリーフ A 後半): a = 1−exp(−add) が既に軟らかい圧縮。白帯の原因は断面形状なので、カーブでは直らない
- 弱い光源ほど τ を短くする(ブリーフ D 後半): 可視長 = τ·ln(gain·I0/ε) なので、I0 の差を付ければ長さの差は自動で出る。個別実装しない
- 光源強度推定に src の sRGB ミップを toLin して使う: sRGB 平均を線形化すると小さな光源が 50 倍以上過小になる。線形輝度テクスチャ(lum, R16F)を別に持つことにした

### 未解決
- v0 の実機確認は設計時に 2 条件(強さ 90 / 明るさ 60)だけ内蔵ブラウザで目視した(白い棒の格子化、窓の一斉発光を確認。DESIGN.md §2)。6 枚の行列画像としての正式な記録は実装の手順 0 で行う
- tauScale の値(初期 0.75)は白点テストで v0 の既定可視長に合わせて決める
- HEIC(iOS の写真アプリ経由)の受け入れは実機で未確認

<!-- 実装も Fable 5.1 が担当した(ユーザー指示で変更) -->

## 2026-09-18 実装(Fable 5.1)

### 決定
- tauScale = 0.55(設計時の仮値 0.75 から変更)。`tests/calib.cjs` で既定値の白点可視長(40/255 しきい値)を v0=98px に対し 103px に揃えた。0.75 だと 140px で 1.4 倍に伸びる
- 可視長のしきい値は 40/255。設計書の 5/255 は sRGB で線形 0.0002 に相当し目に見えないうえ、v0 の白帯比が 0.12 で「合格」になってしまう。40/255 なら v0 は 0.24 で不合格、v1 は 0.05 で合格と、目視の印象と一致する
- v0 には `__cf` フックのほかに、exportImage から renderExport(blob 生成部分)を分離する変更を加えた。挙動は同じ

### ハマった罠
- Edge(Playwright)で描画直後に `browser.close()` を呼ぶと戻ってこないことがあり、node プロセスが残って CPU を食い続ける(4 本で各 1000 秒超)。後続のテストが極端に遅くなる。対策: harness の close に 8 秒の時間制限、`playwright` をコマンドラインに含む msedge だけを止める `killStragglers`、各スクリプト末尾で `process.exit`
- `page.route(...).abort()` はコンソールに `net::ERR_FAILED` を出し「エラー 0 件」の判定を壊す。空の 200 応答で返す
- SwiftShader の描画自体は速い(白点 1440×960 の 1 描画 + readPixels で 2〜4 秒)。遅く見えたら上のプロセス残りを疑う
- 条件5(素通し画素の一致)で見つかった揺れ: add<1e-5 の判定では裾の極めて弱い光が届く画素が pow 往復+ディザで ±1 揺れる。合成の素通し条件を「sRGB の変化が 0.5/255 未満なら元画素」に変更して解消。あわせて「左上の空」は角度 0 のとき左端の街灯の縦光条の真上に当たるため、比較領域を上端中央に変更
- 条件4の指標: 夜景では最大値が飽和し(255)、40/255 の交点は裾の傾きが浅く 2/255 の差で 20px 動く。「足された光(出力−元画像)」の r=10,20,40,80 の値の差 ≤ 8/255 と、100/255 交点の差 ±10% に変更。実測: 値の差 3、長さ 19 vs 20px
